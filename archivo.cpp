#include <iostream>
#include <iomanip>
#include "stdio.h"
#include "stacktables.h"
#include "elementsforscope.h"
#include "tablescope.h"
#include "auxiliaryfunctions.h"
using namespace std;

int main() {
    isString();
    /*TablesStack tb;
    addElement("int","a"," ","vardecl");
    addList(getElement());
    addElement("int","b"," ","vardecl");
    addList(getElement());

    tb.Push(getTable());
    deleteTable();

    addElement("int","main"," ","func");
    addList(getElement());
    tb.Push(getTable());
    deleteTable();

    addElement("int","a"," ","vardecl");
    addList(getElement());
    addElement("b","=","5","varassign");
    addList(getElement());


    tb.Push(getTable());
    deleteTable();

    addElement("void","func"," ","func");
    addList(getElement());

    tb.Push(getTable());
    deleteTable();

    addElement("a","=","6","varassign");
    addList(getElement());

    tb.Push(getTable());

    tb.SetGlobalScope();
    ScopeCheckingVariables(tb);*/

    return 0;

}
